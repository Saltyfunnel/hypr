#
# File: ~/.config/wal/templates/tofi.conf
# This config is generated by Pywal using the 'tofi.conf' template.
#
# Note: Tofi uses hex codes, which Pywal provides in the {colorX} placeholders.
#

# --- Tofi Configuration ---
# Use Pywal colors for theming.

# Background color (using Pywal's {color0})
background-color={color0}

# Text color (using Pywal's {color7} - foreground)
text-color={color7}

# Outline color (using Pywal's {color4} - a primary accent)
outline-color={color4}

# Selection background color (using Pywal's {color8} - a bright background/dark accent)
selection-background-color={color8}

# Selection text color (using Pywal's {color15} - a bright foreground)
selection-text-color={color15}

# --- Standard Tofi Options (Copied from your likely config) ---
font="CaskaydiaCove Nerd Font 13"
width=40%
height=20%
horizontal-padding=100
vertical-padding=20
prompt-padding=5
entry-padding=10
border-width=2
corner-radius=10
