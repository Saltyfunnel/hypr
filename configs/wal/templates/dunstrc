#
# File: ~/.config/wal/templates/dunstrc
# Generated by Pywal. This file is written to ~/.config/dunst/dunstrc
#

# --- Global Settings ---
font = "CaskaydiaCove Nerd Font 10"
# Use a background opacity to allow the wallpaper to show through
background_opacity = 0.95 

# --- Geometry and Behavior ---
width = 300
height = 80
origin = top-right
offset = 10x10
corner_radius = 10
show_age_as_h = true

# --- Default Colors ---
# Pywal's {colorX} placeholders used for dynamic theming
# Note: Colors must be defined *without* the leading '#' in Dunst config!

[global]
    frame_width = 3
    frame_color = "{color4}"  # Accent color for the frame
    
    # Text colors
    default_text_color = "{color7}"

# --- Urgency Low (Information) ---
[urgency_low]
    background = "{color0}"     # Background (Black/Darkest)
    foreground = "{color7}"     # Text (White/Lightest)
    frame_color = "{color2}"    # Accent/Info color

# --- Urgency Normal (Standard) ---
[urgency_normal]
    background = "{color0}"
    foreground = "{color7}"
    frame_color = "{color4}"    # Primary Accent color

# --- Urgency Critical (Alert) ---
[urgency_critical]
    background = "{color1}"     # Red/Error color
    foreground = "{color0}"     # Dark text on bright background for contrast
    frame_color = "{color1}"
